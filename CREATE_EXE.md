# 🔧 Как создать .exe файл для игры

## Способ 1: Использовать готовые скрипты (самый простой)

### Вариант A: Батник (run_game.bat)
1. Двойной клик на файл `run_game.bat`
2. Игра откроется в браузере
3. Готово!

**Преимущества:**
- ✅ Простой способ
- ✅ Работает на всех Windows
- ✅ Не требует установки

**Недостатки:**
- ❌ Показывает черное окно консоли

### Вариант B: VBS скрипт (run_game.vbs)
1. Двойной клик на файл `run_game.vbs`
2. Игра откроется в браузере
3. Готово!

**Преимущества:**
- ✅ Нет черного окна
- ✅ Красивое сообщение
- ✅ Работает на всех Windows

**Недостатки:**
- ❌ Может быть заблокирован антивирусом

## Способ 2: Создать настоящий .exe (требует инструментов)

### Вариант A: Использовать PyInstaller (если установлен Python)

1. **Установи PyInstaller:**
```bash
pip install pyinstaller
```

2. **Создай Python скрипт `launcher.py`:**
```python
import webbrowser
import os
import sys

# Получаем путь к текущей папке
script_dir = os.path.dirname(os.path.abspath(__file__))
html_file = os.path.join(script_dir, 'tiger_game_improved.html')

# Проверяем, существует ли файл
if not os.path.exists(html_file):
    print("Ошибка: Файл tiger_game_improved.html не найден!")
    sys.exit(1)

# Открываем файл в браузере
webbrowser.open('file://' + html_file)
```

3. **Создай .exe:**
```bash
pyinstaller --onefile --windowed --icon=icon.ico launcher.py
```

4. **Готовый .exe будет в папке `dist/`**

### Вариант B: Использовать Electron (для профессионального приложения)

1. **Установи Node.js** с https://nodejs.org/

2. **Создай папку проекта:**
```bash
mkdir tiger-game
cd tiger-game
npm init -y
npm install electron --save-dev
```

3. **Создай файл `main.js`:**
```javascript
const { app, BrowserWindow } = require('electron');
const path = require('path');

function createWindow() {
  const win = new BrowserWindow({
    width: 1400,
    height: 900,
    icon: path.join(__dirname, 'icon.png')
  });

  win.loadFile('tiger_game_improved.html');
}

app.on('ready', createWindow);

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') {
    app.quit();
  }
});
```

4. **Обнови `package.json`:**
```json
{
  "main": "main.js",
  "homepage": "./",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder"
  }
}
```

5. **Установи electron-builder:**
```bash
npm install electron-builder --save-dev
```

6. **Создай .exe:**
```bash
npm run build
```

### Вариант C: Использовать NW.js

1. **Скачай NW.js** с https://nwjs.io/

2. **Создай папку `app`:**
```
app/
├── package.json
├── tiger_game_improved.html
└── index.html (если нужен)
```

3. **Создай `package.json`:**
```json
{
  "name": "tiger-game",
  "main": "tiger_game_improved.html",
  "window": {
    "title": "🐯 Тигрёнок в лесу",
    "width": 1400,
    "height": 900
  }
}
```

4. **Упакуй в .exe** используя инструменты NW.js

## Способ 3: Использовать онлайн сервисы

### Вариант A: Использовать HTA (HTML Application)

1. **Переименуй файл:**
```
tiger_game_improved.html → tiger_game.hta
```

2. **Добавь в начало файла:**
```html
<HTA:APPLICATION
  ID="TigerGame"
  APPLICATIONNAME="Тигрёнок в лесу"
  VERSION="1.0"
  BORDER="thin"
  CAPTION="yes"
  ICON="icon.ico"
  INNERBORDER="no"
  SCROLL="no"
  SCROLLFLAT="yes"
  SINGLEINSTANCE="yes"
  SYSMENU="yes"
  WINDOWSTATE="normal"
  WIDTH="1400"
  HEIGHT="900"
/>
```

3. **Двойной клик на файл .hta**
4. **Готово!**

## Рекомендуемый способ

### Для быстрого решения:
👉 **Используй `run_game.vbs`** - просто, быстро, работает

### Для профессионального приложения:
👉 **Используй Electron** - красиво, профессионально, кроссплатформенно

### Для простого решения:
👉 **Используй HTA** - просто, встроено в Windows

## Как создать иконку?

1. Найди красивую иконку тигра
2. Сохрани как `icon.ico` (32x32 пиксела)
3. Используй в скриптах выше

## Проблемы и решения

### Проблема: Антивирус блокирует .exe
**Решение:** Используй `run_game.vbs` вместо .exe

### Проблема: Браузер не открывается
**Решение:** Проверь, что `tiger_game_improved.html` в той же папке

### Проблема: Черное окно консоли
**Решение:** Используй `run_game.vbs` вместо `run_game.bat`

## Итоги

| Способ | Сложность | Результат | Рекомендация |
|--------|-----------|-----------|--------------|
| run_game.bat | ⭐ | Батник | Для быстрого старта |
| run_game.vbs | ⭐ | VBS скрипт | ✅ Рекомендуется |
| HTA | ⭐⭐ | .hta файл | Для Windows |
| PyInstaller | ⭐⭐⭐ | .exe файл | Если есть Python |
| Electron | ⭐⭐⭐⭐ | .exe файл | Для профессионального приложения |
| NW.js | ⭐⭐⭐⭐ | .exe файл | Для профессионального приложения |

## Быстрый старт

**Самый простой способ:**
1. Двойной клик на `run_game.vbs`
2. Игра откроется
3. Готово!

**Готово! 🐯🎮**
