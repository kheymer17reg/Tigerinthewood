# 🔐 Админ-панель для игры "Тигрёнок в лесу"

## Что это?

Админ-панель - это специальный инструмент для управления игрой, уровнями и пользователями. Она защищена паролем и позволяет:

- 📋 Управлять уровнями пользователей
- 👥 Отслеживать активных пользователей
- 📊 Смотреть статистику
- ⚙️ Менять настройки

## Как использовать?

### Шаг 1: Добавить админ-панель в игру

1. Открой файл `tiger_game_improved.html`
2. Добавь перед закрывающим тегом `</head>`:
```html
<link rel="stylesheet" href="admin_panel.css">
```

3. Добавь перед закрывающим тегом `</body>`:
```html
<script src="admin_panel.js"></script>
```

4. Добавь кнопку админ-панели в header (после других кнопок):
```html
<button class="admin-btn" onclick="openAdminPanel()">🔐 Админ</button>
```

### Шаг 2: Инициализировать админ-панель

В конце JavaScript кода (в функции `window.onload`), добавь:
```javascript
// Инициализация админ-панели
initAdminPanel();
```

### Шаг 3: Отслеживать пользователей

В функции `startGame()`, добавь:
```javascript
// Отслеживание пользователя
trackUser(playerName, 1, 0);
```

В функции `checkWin()`, добавь:
```javascript
// Отслеживание завершенного уровня
trackCompletedLevel(playerName, game.level);
```

## Пароль

**Пароль по умолчанию:** `admin123`

**Как изменить пароль:**

1. Открой файл `admin_panel.js`
2. Найди строку:
```javascript
const ADMIN_PASSWORD = "admin123";
```

3. Измени на свой пароль:
```javascript
const ADMIN_PASSWORD = "твой_пароль";
```

## Функции админ-панели

### 📋 Вкладка "Уровни"

**Загрузить уровень:**
- Выбери JSON файл с уровнем
- Нажми "Загрузить"
- Уровень будет сохранен в базе

**Уровни пользователей:**
- Список всех загруженных уровней
- Информация об авторе и размере
- Кнопки для удаления и скачивания

### 👥 Вкладка "Пользователи"

**Активные пользователи:**
- Список всех пользователей, которые играли
- Текущий уровень каждого
- Количество очков
- Время последней активности
- Кнопка для удаления пользователя

### 📊 Вкладка "Статистика"

**Общая статистика:**
- Всего пользователей
- Всего загруженных уровней
- Всего пройденных уровней

### ⚙️ Вкладка "Настройки"

**Опции:**
- Разрешить/запретить загрузку уровней
- Выход из админ-панели

## Как работает отслеживание?

### Отслеживание пользователей

Когда пользователь начинает игру, вызывается функция:
```javascript
trackUser(playerName, level, score);
```

Это сохраняет информацию о пользователе в `localStorage`:
```json
{
  "name": "Иван",
  "level": 1,
  "score": 0,
  "completedLevels": [],
  "lastActive": "11.02.2024 12:30:45"
}
```

### Отслеживание завершенных уровней

Когда пользователь проходит уровень, вызывается функция:
```javascript
trackCompletedLevel(playerName, levelNumber);
```

Это добавляет номер уровня в массив `completedLevels`.

## Хранение данных

Все данные хранятся в `localStorage` браузера:

- `users_list` - список пользователей
- `user_levels` - загруженные уровни
- `tiger_admin_logged_in` - статус входа админа

## Безопасность

⚠️ **Важно:**
- Пароль хранится в коде (не безопасно для продакшена)
- Данные хранятся в браузере (не синхронизируются между устройствами)
- Для серьезного проекта используй сервер!

## Как сделать более безопасным?

### Вариант 1: Использовать более сложный пароль

```javascript
const ADMIN_PASSWORD = "Tr0p1c@lT1g3r2024!";
```

### Вариант 2: Использовать сервер (Node.js + Express)

```javascript
// Отправить пароль на сервер
fetch('/api/admin/login', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({password: password})
})
.then(response => response.json())
.then(data => {
    if (data.success) {
        sessionStorage.setItem('admin_token', data.token);
    }
});
```

### Вариант 3: Использовать двухфакторную аутентификацию

```javascript
// Требовать пароль + код из email
```

## Примеры использования

### Пример 1: Загрузить уровень

1. Нажми кнопку "🔐 Админ"
2. Введи пароль `admin123`
3. Перейди на вкладку "📋 Уровни"
4. Выбери JSON файл с уровнем
5. Нажми "Загрузить"

### Пример 2: Посмотреть статистику

1. Нажми кнопку "🔐 Админ"
2. Введи пароль
3. Перейди на вкладку "📊 Статистика"
4. Смотри количество пользователей и уровней

### Пример 3: Удалить пользователя

1. Нажми кнопку "🔐 Админ"
2. Введи пароль
3. Перейди на вкладку "👥 Пользователи"
4. Нажми "Удалить" рядом с пользователем

## Интеграция с основной игрой

### Шаг 1: Скопируй файлы

```
tiger_game_improved.html
admin_panel.js
admin_panel.css
```

### Шаг 2: Добавь ссылки в HTML

```html
<!DOCTYPE html>
<html>
<head>
    ...
    <link rel="stylesheet" href="admin_panel.css">
</head>
<body>
    ...
    <button class="admin-btn" onclick="openAdminPanel()">🔐 Админ</button>
    ...
    <script src="admin_panel.js"></script>
</body>
</html>
```

### Шаг 3: Добавь отслеживание в JavaScript

```javascript
// В функции startGame()
trackUser(playerName, 1, 0);

// В функции checkWin()
trackCompletedLevel(playerName, game.level);
```

## Часто задаваемые вопросы

**Q: Где хранятся данные?**
A: В `localStorage` браузера. Они не синхронизируются между устройствами.

**Q: Как экспортировать данные?**
A: Используй кнопку "Скачать" для каждого уровня.

**Q: Как импортировать данные?**
A: Используй кнопку "Загрузить" на вкладке "Уровни".

**Q: Как изменить пароль?**
A: Отредактируй `admin_panel.js` и измени `ADMIN_PASSWORD`.

**Q: Как удалить все данные?**
A: Очисти `localStorage` в браузере (F12 → Application → Local Storage).

**Q: Можно ли использовать на сервере?**
A: Да, но нужно добавить серверную часть для безопасности.

## Дальнейшее развитие

### Идеи для улучшения:

1. **Серверная часть:**
   - Сохранять данные на сервере
   - Синхронизировать между устройствами
   - Использовать базу данных

2. **Расширенная статистика:**
   - Графики прогресса
   - Рейтинги пользователей
   - Анализ сложности уровней

3. **Модерация:**
   - Одобрение уровней перед публикацией
   - Система рейтинга уровней
   - Блокировка неподходящих уровней

4. **Безопасность:**
   - Двухфакторная аутентификация
   - Шифрование паролей
   - Логирование действий

## Поддержка

Если у тебя есть вопросы:
1. Прочитай этот файл еще раз
2. Проверь консоль браузера (F12) на ошибки
3. Убедись, что все файлы в одной папке

## Удачи! 🐯🎮
